/* exported take */
/*
  define a function called take with two parameters, array and count
    take the array, return a portion of the array into a new array from the start of the array index and the count index .
*/

function take(array, count) {
  return array.slice(0, count);
}
