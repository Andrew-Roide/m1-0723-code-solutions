/* exported getValue */
/*
  define a function called getValue that has two parameters, object and key
    access the property value of the object using bracket notation and return it.
*/
function getValue(object, key) {
  return object[key];
}
