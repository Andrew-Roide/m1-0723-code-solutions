/* exported head */

/*
  define a function called head with one parameter called array
  get the first index of the array parameter and set it to a variable called firstIndex
  return the value of firstIndex
*/

function head(array) {
  const firstIndex = array[0];
  return firstIndex;
}
